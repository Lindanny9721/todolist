<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Todo</title>
</head>
<body>
    <h1>Edit Todo</h1>

    <form action="/todos/edit/<%= index %>" method="POST">
        <input type="text" name="task" value="<%= todo.task %>" required>
        <select name="priority">
            <option value="High" <% if (todo.priority === 'High') { %> selected <% } %>>High</option>
            <option value="Medium" <% if (todo.priority === 'Medium') { %> selected <% } %>>Medium</option>
            <option value="Low" <% if (todo.priority === 'Low') { %> selected <% } %>>Low</option>
        </select>
        <button type="submit">Update Todo</button>
    </form>

    <br>
    <a href="/todos">Back to Todo List</a>
</body>
</html>